# Production config for ninfer. PREREQUISITE: Wallpaper Engine must NOT be running
# (disabled from startup 2026-08-21). If it is, use the WE-tolerant fallbacks below.
# Option B (DEFAULT, max text): CTX=262144 VISION=0  -- native model max; validated WE-free:
#   needle 3/3 @ 240K, prefill 2,238 tok/s @ 240K, decode 142 tok/s @ 240K ctx, MTP 84%
# Option A (vision):            CTX=172032 VISION=1  -- validated WE-free: prefill 5,945 tok/s
#   @ 40K (full par with text), decode 170.8, images 2/2
# WE-tolerant fallbacks (survive desktop VRAM pigs): CTX=252928 VISION=0 | CTX=152576 VISION=1
# NEVER: vision with CTX>=192512 -- workspace starvation, 7-11x slow prefill (WE-independent).
CTX=262144
VISION=0
